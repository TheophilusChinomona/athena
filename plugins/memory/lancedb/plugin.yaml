name: lancedb
version: 1.1.0
description: "LanceDB vector memory — LLM Smart Extraction, 3-tier system, dedup pipeline, hybrid BM25+vector recall."
pip_dependencies:
  - "hermes-memory-lancedb>=1.1.0"
requires_env:
  - OPENAI_API_KEY
hooks:
  - on_session_end
  - on_pre_compress
  - on_memory_write
