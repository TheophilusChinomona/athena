name: lancedb
version: 1.1.0
description: "LanceDB vector memory — hybrid BM25+vector recall with recency decay."
pip_dependencies:
  - "hermes-memory-lancedb>=1.1.0"
requires_env:
  - OPENAI_API_KEY
